<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Voluntário</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .btn-primary {
            color: black;
            background-color: #f5b942;
            border-color: #f5b942;
        }
    </style>
</head>

<body>
    <div class="card card-warning">
        <div class="card-header">
            <h3 class="card-title">Editar Voluntário</h3>
        </div>

        <div class="card-body">
            <form action="/voluntarios/<%= voluntario.Pessoa_id %>" method="POST" class="needs-validation" novalidate>
                <div class="row">
                    <div class="col-sm-6">
                        <div class="form-group">
                            <label for="voluDisponibilidade">Disponibilidade Mensal (Horas):</label>
                            <input type="number" class="form-control" id="voluDisponibilidade" name="voluDisponibilidade" placeholder="Disponibilidade" value="<%= voluntario.voluDisponibilidade %>" required>
                            <div class="invalid-feedback">Por favor, insira a disponibilidade do voluntário.</div>
                        </div>
                    </div>
                    <div class="col-sm-6">
                        <div class="form-group">
                            <label for="voluHabilidades">Habilidades:</label>
                            <input type="text" class="form-control" id="voluHabilidades" name="voluHabilidades" placeholder="Habilidades" value="<%= voluntario.voluHabilidades %>">
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col-sm-6">
                        <div class="form-group">
                            <label for="voluPrefeAtuacao">Preferência de Atuação:</label>
                            <input type="text" class="form-control" id="voluPrefeAtuacao" name="voluPrefeAtuacao" placeholder="Preferência de Atuação" value="<%= voluntario.voluPrefeAtuacao %>">
                        </div>
                    </div>
                    <div class="col-sm-6">
                        <div class="form-group">
                            <label for="Pessoa_id">ID da Pessoa:</label>
                            <input type="number" class="form-control" id="Pessoa_id" name="Pessoa_id" placeholder="ID da Pessoa" value="<%= voluntario.Pessoa_id %>" required>
                            <div class="invalid-feedback">Por favor, insira o ID da pessoa.</div>
                        </div>
                    </div>
                </div>
                <button type="submit" class="btn btn-primary">Salvar</button>
            </form>
        </div>
    </div>

    <script>
        (function () {
            'use strict';
            window.addEventListener('load', function () {
                var forms = document.getElementsByClassName('needs-validation');
                var validation = Array.prototype.filter.call(forms, function (form) {
                    form.addEventListener('submit', function (event) {
                        if (form.checkValidity() === false) {
                            event.preventDefault();
                            event.stopPropagation();
                        }
                        form.classList.add('was-validated');
                    }, false);
                });
            }, false);
        })();
    </script>
</body>

</html>